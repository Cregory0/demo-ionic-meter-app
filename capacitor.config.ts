import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-meter-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
